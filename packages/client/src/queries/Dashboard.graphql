query DashboardStats {
  dashboardStats {
    dataSources
    forumPosts
    uploads
    uploadedBytes
    projects
    users
    sketches
    forumPosts
  }
  activeProjects {
    id
    name
    url
    logoUrl
    dataHostingQuotaUsed
    numUsers
    numSketches
    numDataSources
    numForumPosts
    numUploads
    activity(period: _24HRS) {
      registeredUsers
      totalSketches
      totalForumPosts
      totalDataSources
      totalUploadedLayers
      uploadsStorageUsed
      newUsers
      newSketches
      newForumPosts
      newDataSources
      newUploadedBytes
    }
  }
}

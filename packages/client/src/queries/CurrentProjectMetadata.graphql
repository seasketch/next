query CurrentProjectMetadata($slug: String!) {
  projectBySlug(slug: $slug) {
    id
    slug
    url
    name
    description
    logoLink
    logoUrl
    accessControl
    sessionIsAdmin
    isFeatured
  }
}

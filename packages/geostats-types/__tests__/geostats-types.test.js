'use strict';

const geostatsTypes = require('..');
const assert = require('assert').strict;

assert.strictEqual(geostatsTypes(), 'Hello from geostatsTypes');
console.info('geostatsTypes tests passed');

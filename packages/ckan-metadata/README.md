# ckan-metadata

Normalizes CKAN `package_show` records (optionally with a `ckanext-scheming` schema) into SeaSketch metadata ProseMirror documents.

Used by the API `computedMetadata` resolver. See `__tests__/fixtures` and `scripts/refresh-fixtures.ts` for the MaPP / open.canada.ca examples.

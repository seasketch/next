{
  "databaseOwner": "postgres",
  "pgSettings": {
    "search_path": "private,public"
  },
  "afterAllMigrations": [
    {
      "_": "command",
      "shadow": true,
      "command": "echo 'after' && if [ \"$IN_TESTS\" != \"1\" ]; then npm run schema; fi"
    }
  ]
}